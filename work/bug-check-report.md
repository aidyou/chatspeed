# Bug 文档校对报告

生成时间：2026-03-14
文档：`work/bug.md`

---

## ✅ 已完成的项目

### 1. Workflow.vue Bug 修复 - 部分完成

#### ✅ 1.1 审批状态还原后无法关闭窗口
**状态：需要进一步验证**
- 后端代码显示已正确处理审批状态（`WorkflowState::Cancelled`）
- 日志显示 "No sender for session" 表明会话管理可能存在问题
- 需要测试验证前端是否正确处理会话结束状态

#### ✅ 1.2 审批模式问题
**状态：已完成**
- ✅ 前端 i18n 已更新为「智能审批 (只拦截风险操作)」
  - 位置：`src/i18n/locales/zh-Hans.json:176`
- ✅ 后端已应用审批模式设置
  - `interceptors.rs:259-340` 显示 Smart 和 Full 模式的正确逻辑
  - Smart 模式会检查是否为只读命令
  - Full 模式自动批准高风险命令

#### ✅ 1.3 数据库结构优化
**状态：已完成**
- ✅ v5 迁移已添加 `agent_config` 字段到 `workflows` 表
- ✅ 保留了 `shell_policy` 等字段到 `agents` 表
- 注意：`allowed_paths` 和 `final_audit` 仍保留在 agents 表中，未合并

---

### 2. Workflow.vue 页面优化 - 部分完成

#### ⚠️ 2.1 edit_file, write_file 的 diff 显示
**状态：需要验证**
- 前端代码显示 `displayType === 'diff'` 时使用 `MarkdownSimple` 渲染
- 需要验证 diff 格式是否正确

#### ✅ 2.2 工具调用信息展示格式
**状态：已完成**
- 前端已显示命令详情：`<pre v-else class="raw-content">`
- 后端已提供命令详情：`format!("Command: {}\nReason: {}", command_str, reason)`

#### ⚠️ 2.3 前端用户消息强制换行
**状态：需要验证**
- 当前样式：`white-space: pre-wrap; word-break: break-all;` (Workflow.vue:2504-2505)
- 需要验证是否在长连续字符情况下正确换行

#### ⚠️ 2.4 /models 模型配置前端生效问题
**状态：需要验证**
- `WorkflowModelSelector.vue` 组件存在
- `onModelConfigSave` 函数存在 (Workflow.vue:516)
- 需要验证配置是否正确保存到数据库并在后续对话中使用

---

### 3. Agent.vue 模型配置优化

#### ⚠️ 3.1 去掉不必要的模型配置
**状态：需要验证**
- 前端 `Agent.vue` 仍显示多个模型角色（plan, act, vision）
- 需要检查 Rust 端是否删除了相关代码

---

### 4. 后端优化 - 部分完成

#### ✅ 4.1 生成工作流标题异步化
**状态：已完成**
- `engine.rs:419-430` 显示使用 `tokio::spawn` 异步生成标题
- 不阻塞主线程

#### ⚠️ 4.2 步数耗尽流程优化
**状态：部分完成**
- ✅ 步数耗尽时发送系统提醒 (engine.rs:911-936)
- ✅ 80% 步数时警告 (engine.rs:932-956)
- ❌ 未实现 ask_user 询问用户是否继续
- ❌ 未实现 Full 模式下自动增加步数

#### ⚠️ 4.3 Shell 运行结果即时传递
**状态：需要研究**
- 需要研究可行性

#### ⚠️ 4.4 自动审批命令执行详情
**状态：需要验证**
- 需要验证自动审批的命令是否向前端发送执行详情
- 需要验证工具状态是否正确更新为完成

#### ⚠️ 4.5 用户取消操作错误级别
**状态：需要修改**
- 当前代码将取消操作标记为 `WorkflowEngineError::Cancelled`
- 日志显示 "Critical Error" 可能是前端显示问题
- 需要修改错误处理逻辑，不将取消视为关键错误

#### ⚠️ 4.6 上下文压缩问题
**状态：需要验证**
- 需要验证压缩后下一轮是否再次触发压缩

#### ⚠️ 4.7 自动审批命令规则优化
**状态：需要实现**
- 需要实现智能规则匹配（如 `cat *` 匹配 `cat /a/b/c`）
- 需要将全部同意的规则更新到 workflow 表的 `shell_policy` 字段

---

## 📋 遗漏或需要进一步处理的问题

### 高优先级

1. **步数耗尽流程不完整** (4.2)
   - 缺少 ask_user 询问机制
   - 缺少 Full 模式自动增加步数逻辑

2. **用户取消操作错误级别** (4.5)
   - 需要修改前端显示，不将取消显示为"Critical Error"

3. **模型配置前端生效问题** (2.4)
   - 需要验证配置保存和使用逻辑

### 中优先级

4. **diff 显示格式** (2.1)
   - 需要验证 edit_file, write_file 的 diff 显示是否正确

5. **用户消息换行** (2.3)
   - 需要验证长连续字符的换行问题

6. **自动审批命令详情** (4.4)
   - 需要验证执行详情是否传递给前端

7. **上下文压缩重复触发** (4.6)
   - 需要验证压缩逻辑

### 低优先级

8. **Shell 运行结果即时传递** (4.3)
   - 需要研究可行性

9. **自动审批规则优化** (4.7)
   - 需要实现智能规则匹配和更新

---

## 🔍 总结

| 类别 | 已完成 | 部分完成 | 未开始/需验证 |
|------|--------|----------|---------------|
| Workflow.vue Bug 修复 | 2 | 1 | 0 |
| Workflow.vue 页面优化 | 1 | 0 | 3 |
| Agent.vue 优化 | 0 | 0 | 1 |
| 后端优化 | 1 | 1 | 5 |
| **总计** | **4** | **2** | **9** |

**整体完成度：约 33%** (4/15 完全完成，2/15 部分完成)

---

## 📝 建议

1. **立即处理**：步数耗尽流程、用户取消错误显示、模型配置生效问题
2. **近期处理**：diff 显示验证、消息换行验证、自动审批详情传递
3. **后续优化**：Shell 结果即时传递、智能规则匹配

---

*报告生成完毕*
