# Workflow Storage Model

The durable rules from this note have been moved into
`src-tauri/src/workflow/react/CONSTITUTION.md`.

This file is now only a temporary working note under `work/` and may be removed
when the workflow module cleanup is considered complete.
