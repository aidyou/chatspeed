@GEMINI.md

- Prefer existing variables from `src/style/element/css-vars.css` for CSS styles. For spacing, radii, font sizes, and similar values, use the closest semantic `--cs-*` token instead of hard-coded numbers whenever possible.
