schema_version: 1
builtin_id: coding
builtin_version: 5
name: Coding
description: A focused implementation agent for coding, debugging, refactoring, and delivering production-quality changes. Best used when the task is already understood and requires concrete code edits, validation, and follow-through.
role: primary
disabled: true
prompts:
  system: system.md
  planning: planning.md
  image_recognition: image-recognition.md
config:
  shell_policy: default
  approval_level: default
  auto_approve:
    - glob
    - grep
    - list_dir
    - read_file
    - web_fetch
    - web_search
  available_tools:
    - bash
    - edit_file
    - glob
    - grep
    - list_dir
    - read_file
    - web_fetch
    - web_search
    - write_file
  final_audit: false
  skill_enabled: true
  selected_skills:
    - commit
  phase: standard
